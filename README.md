# ticketing-system
IT help desk ticketing system using React + ASP.NET Core 8 using Minimal API's + SQLite, hosted on IIS. Employees raise tickets with rich-text/screenshots; IT triages via Kanban board, assigns, resolves and notifies via Outlook.
